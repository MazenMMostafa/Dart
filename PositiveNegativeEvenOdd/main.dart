void main() {
  int number =4;
  if (number > 0) {
    //number is positive
    //checking the number if even or odd
    number % 2 == 0 ? print("Positive Even") : print("Positive Odd");
  } else {
    //number is negative
    // checking the number is even or odd
    number % 2 == 0 ? print("Negative Even") : print("Negative Odd");
  }
}
